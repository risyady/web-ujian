{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "builder": "NIXPACKS"
    },
    "deplioy": {
        "startCommand": "chmod -R 775 storage bootstrap/cache && php artisan storage:link && php artisan serve --host 0.0.0.0 --port $PORT"
    }
}